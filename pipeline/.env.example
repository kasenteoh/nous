# Pipeline environment configuration.
# Copy to `.env` (gitignored) and fill in the values for local development.
# Production values are stored as GitHub Actions secrets and Vercel env vars.

# ─────────────────────────────────────────────────────────────────────────────
# Required
# ─────────────────────────────────────────────────────────────────────────────

# Postgres connection URL. Must use the `postgresql+psycopg://` scheme
# (psycopg 3 async driver, used by SQLAlchemy 2.x async).
#
# For local dev with a Docker Postgres:
#   docker run --rm -d --name nous-pg -e POSTGRES_PASSWORD=postgres -p 5432:5432 postgres:15
#   DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/postgres
#
# For Supabase: use the **Session pooler** connection string (port 5432,
# IPv4) and rewrite the scheme to `postgresql+psycopg://`. The direct/IPv6
# endpoint does not work from GitHub Actions runners.
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/postgres

# SEC EDGAR requires a User-Agent header containing a contact email.
# Anonymous traffic is blocked.
SEC_USER_AGENT=nous-project your-email@example.com

# ─────────────────────────────────────────────────────────────────────────────
# Optional overrides (defaults shown)
# ─────────────────────────────────────────────────────────────────────────────

# Industry groups to keep from Form D filings. JSON array string.
# Values must match the Form D XSD industryGroupType enum (NOT the EDGAR UI labels).
# INDUSTRY_GROUPS='["Computers","Other Technology","Telecommunications"]'

# Extra days to look back beyond the requested window, to catch late filings
# and EDGAR indexing delays.
# EDGAR_OVERLAP_DAYS=14

# EDGAR rate-limit ceiling. SEC permits up to 10 req/s; default leaves headroom.
# EDGAR_REQUESTS_PER_SECOND=5.0

# ─────────────────────────────────────────────────────────────────────────────
# M2+ — homepage scraping + LLM enrichment
# ─────────────────────────────────────────────────────────────────────────────

# Gemini API key. Required when LLM_PROVIDER=gemini (default).
# Get one at https://ai.google.dev/. Note: gemini-2.5-flash free tier is
# 20 requests/day per project — confirmed in-prod, not the older 1500 figure.
GEMINI_API_KEY=

# LLM provider selection. "gemini" (default; free, but only 20 RPD on
# gemini-2.5-flash) or "deepseek" (paid; ~$0.27/1M input + $1.10/1M output;
# breaks the spec rule "free tier first" — use when throughput beats cost).
# LLM_PROVIDER=gemini

# Required when LLM_PROVIDER=deepseek. Get one at
# https://platform.deepseek.com/api_keys
DEEPSEEK_API_KEY=

# ─────────────────────────────────────────────────────────────────────────────
# M3 — news ingestion + funding extraction + VC portfolio discovery
# ─────────────────────────────────────────────────────────────────────────────

# pg_trgm similarity threshold for fuzzy company-name matching in
# auto_create_company (VC portfolios, news, TechCrunch). 0.85 is the M3
# default; lower catches more near-misses, raise reduces false positives.
# COMPANY_FUZZY_MATCH_THRESHOLD=0.85

# ─────────────────────────────────────────────────────────────────────────────
# Used by later milestones; leave blank for M3
# ─────────────────────────────────────────────────────────────────────────────

SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
VERCEL_DEPLOY_HOOK_URL=
